/**
 * Biometric app lock — M-26.
 *
 * `capacitor-native-biometric`. Opt-in, configurable grace period, passcode
 * fallback. Backgrounding past the grace period prompts on return.
 *
 * iOS string (M-28): NSFaceIDUsageDescription.
 */
export {};
