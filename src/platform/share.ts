/**
 * Native share — M-27.
 *
 * The system share sheet on native; `navigator.share` with a copy-link fallback
 * on web. External links open through `@capacitor/browser` (M-22), never by
 * navigating the web view away from the app.
 */
export {};
