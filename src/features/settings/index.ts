/**
 * Settings.
 *
 * Not one of the five product views — reached from the shell chrome.
 *
 * Tickets that add entries here:
 *   M-12  service-worker update prompt ("تحديث متاح")
 *   M-26  biometric app lock (opt-in, grace period, passcode fallback)
 *   M-29  in-app account deletion — mandatory under Apple Guideline 5.1.1(v)
 *   M-30  data export to JSON
 *   M-31  links to the published privacy policy and terms
 */

export {};
